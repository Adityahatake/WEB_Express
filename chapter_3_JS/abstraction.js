// JavaScript is a high-level, interpreted programming language.
// It is mainly used for adding interactivity to web pages, but can also be used for server-side development (Node.js).

// Example: A simple JavaScript program that prints a message
console.log("Hello, JavaScript!");

// JavaScript supports variables, functions, objects, and more:

// Variables
let name = "Alice";
let age = 25;

// Function
function greet(person) {
    return "Hello, " + person + "!";
}

console.log(greet(name)); // Output: Hello, Alice!

// Object
let user = {
    name: "Bob",
    age: 30,
    isAdmin: false
};

console.log(user.name); // Output: Bob

// JavaScript is dynamically typed, supports first-class functions, and is widely used for web development.